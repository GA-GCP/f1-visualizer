# Semgrep's own default excludes are replaced, not extended, by this file, so the
# usual ones are restated.
node_modules/
dist/
build/
target/
.git/
.terraform/
.terragrunt-cache/
coverage/
*.min.js
*.lock
yarn.lock

# The rule test fixtures under .semgrep/ contain deliberate violations — that is
# what `semgrep --test` asserts against. Scanning them would report every rule
# against its own test.
.semgrep/

# Generated by the Maven flatten plugin.
**/.flattened-pom.xml

# Playwright's own report output.
frontend/playwright-report/
frontend/test-results/
