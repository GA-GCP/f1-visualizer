spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test-issuer.example.com/
          audiences: test.api.f1visualizer.com
  cloud:
    gcp:
      bigquery:
        dataset-name: test_dataset

logging:
  level:
    root: WARN
