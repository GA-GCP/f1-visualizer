spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test-issuer.example.com/
          audiences: test.api.f1visualizer.com

logging:
  level:
    root: WARN
