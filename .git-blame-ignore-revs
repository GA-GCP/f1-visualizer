# Commits that are pure formatting. `git blame --ignore-revs-file` skips them so
# blame still points at whoever last changed the code, not at the reformat.
#
# GitHub reads this file automatically. Locally, opt in once with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style(frontend): adopt Prettier and an EditorConfig
6839cc289102aa58e9918de5a8d126ea0bf6f326
