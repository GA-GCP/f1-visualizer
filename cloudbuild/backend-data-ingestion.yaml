# ==========================================
# Pipeline: f1v-service-data-ingestion
# ==========================================
# Scoped build + test, layer extraction, security scan,
# Kaniko cached image build, and Cloud Run deployment.
# ==========================================
serviceAccount: 'projects/$PROJECT_ID/serviceAccounts/sa-f1v-cloudbuild-${_ENV}@$PROJECT_ID.iam.gserviceaccount.com'

substitutions:
  _ENV: 'dev'
  _SHORT_SHA: 'manual-build'

options:
  logging: CLOUD_LOGGING_ONLY

steps:
  # 1. Scoped Maven Build + Test (only this module and its dependencies)
  - name: 'maven:3.9.12-eclipse-temurin-25'
    id: 'build-and-test'
    entrypoint: 'mvn'
    args:
      - 'clean'
      - 'package'
      - '-pl'
      - 'f1v-service-data-ingestion'
      - '-am'
    dir: 'backend'
    volumes:
      - name: 'm2-cache'
        path: '/root/.m2'

  # 2. Extract Spring Boot JAR Layers
  - name: 'eclipse-temurin:25-jdk'
    id: 'extract-layers'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        mkdir -p /workspace/staging
        cd /workspace/staging
        java -Djarmode=layertools -jar /workspace/backend/f1v-service-data-ingestion/target/*.jar extract

  # 3. Trivy Filesystem Security Scan
  - name: 'aquasec/trivy:latest'
    id: 'security-scan'
    args:
      - 'filesystem'
      - '--exit-code'
      - '1'
      - '--severity'
      - 'CRITICAL,HIGH'
      - '--no-progress'
      - '/workspace/staging'

  # 4. Kaniko Build + Push (with layer caching)
  - name: 'gcr.io/kaniko-project/executor:latest'
    id: 'build-push-image'
    args:
      - '--dockerfile=/workspace/backend/Dockerfile.ci'
      - '--context=/workspace/staging'
      - '--destination=us-central1-docker.pkg.dev/$PROJECT_ID/f1v-repo/data-ingestion:${_SHORT_SHA}'
      - '--destination=us-central1-docker.pkg.dev/$PROJECT_ID/f1v-repo/data-ingestion:latest'
      - '--cache=true'
      - '--cache-repo=us-central1-docker.pkg.dev/$PROJECT_ID/f1v-repo/data-ingestion-cache'

  # 5. Deploy to Cloud Run (immutable SHA tag)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'services'
      - 'update'
      - 'f1v-service-data-ingestion-${_ENV}'
      - '--image=us-central1-docker.pkg.dev/$PROJECT_ID/f1v-repo/data-ingestion:${_SHORT_SHA}'
      - '--region=us-central1'
      - '--project=$PROJECT_ID'
      - '--quiet'

images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/f1v-repo/data-ingestion:${_SHORT_SHA}'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/f1v-repo/data-ingestion:latest'
