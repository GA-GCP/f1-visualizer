server:
  port: 8083
spring:
  cloud:
    gcp:
      firestore:
        project-id: f1v-example-project
        database-id: (default)
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://example-tenant.us.auth0.com/.well-known/jwks.json
          audiences: dev.api.f1visualizer.com