spring:
  data:
    redis:
      host: ${SPRING_REDIS_HOST}
      port: ${SPRING_REDIS_PORT}
      timeout: 10000ms
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://example-okta-org.okta.com/oauth2/example-auth-server-prod
          audiences: api.f1visualizer.com